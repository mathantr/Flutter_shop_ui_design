import 'package:flutter/material.dart';

class Voucer extends StatelessWidget {
  const Voucer({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Voucer'),
      ),
    );
  }
}
